	Вариант 1:
1.1. Вывести все номера аквариумов, в которых живут все виды рыб
1.2. R1 = pi вид_рыбы (P2); 
	 R  = pi N_авквариума(P4/R1);
1.3. SELECT DISTINCT a.N_авквариума
	 FROM P4 a
	 WHERE NOT EXISTS (SELECT b.вид_рыбы
					   FROM P2 b
					   WHERE NOT EXISTS(SELECT c.вид_рыбы
										FROM P4 c
										WHERE a.N_аквариума = c.N_авквариума AND b.вид_рыбы = c.вид_рыбы));
2.1. SELECT вид_рыбы
	 FROM P2
	 WHERE страна_происхождения = "Бирма";
	 R = pi вид_рыбы (sigma страна_происхождения = "Бирма" (P2));
2.2. CREATE VIEW P5 AS SELECT *
					   FROM P2
					   WHERE код_содержания IN (SELECT код_содержания
												FROM P3
												WHERE минимальная_температура > 20);
	 На РА невозможно создавать представления, выборка нужных данных:
	 R1 = pi код_содержания (sigma минимальная_температура > 20 (P3));
	 R  = pi вид_рыбы, вид_корма, код_содержания, страна_происхождения (sigma код_содержания !принадлежит! R1 (P2));
	 
	Вариант 2:
1.1. Вывести вид рыбы и тип флоры для рыб из Поленезии
1.2. R1 = sigma страна_происхождения = "Полинезия" (P2);
	 R  = pi вид_рыбы, тип_флоры (R1 |><| P3);
1.3. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE a.страна_происхождения = "Поленезия";
2.1. SELECT N_авквариума
	 FROM P4
	 WHERE вид_рыбы = "гуппи";
	 R = pi N_авквариума (sigma вид_рыбы = "гуппи" (P4));
2.2. UPDATE P2
	 SET страна_происхождения = "Уругвай"
	 WHERE страна_происхождения = "Башкирия";
	 На РА невозможно редактировать данные, выборка нужных данных:
	 R = pi вид_рыбы, страна_происхождения (sigma страна_происхождения = "Башкирия" (P2));
	 
	Вариант 3:
1.1. Вывести номера аквариумов с температурой не меньше 20 градусов и общее количество рыб не больше 50, в которых живут гуппи
1.2. R1 = pi N_авквариума (sigma температура >= 20 ^ общее_количество_рыб <= 50 (P1));
	 R2 = pi N_авквариума (sigma вид_рыбы = "гуппи" (P4));
	 R  = R1 !пересечение! R2;
1.3. SELECT a.N_авквариума
	 FROM P1 a 
	 WHERE a.температура >= 20
	 AND общее_количество_рыб <= 50
	 AND N_авквариума IN (SELECT b.N_авквариума
					      FROM P4 b
					      WHERE вид_рыбы = "гуппи");
2.1. SELECT a.температура  
	 FROM P1 a
	 WHERE a.N_авквариума IN (SELECT b.N_авквариума
							  FROM P4 b
							  WHERE b.вид_рыбы IN (SELECT c.вид_рыбы
												   FROM P2 c
												   WHERE c.страна_происхождения = "Южная Америка"));
	
	R = pi температура (sigma страна_происхождения = "Южная Америка" (P1 |><| P4 |><| P2));
2.2. DELETE FROM P1
	 WHERE N_авквариума = 13;
	 Если установлены правила целостности, а именно ON DELETE CASCADE след. запрос не требуется
	 DELETE FROM P4
	 WHERE N_авквариума = 13;
	 На РА невозможно удлять данные, выборка нужных данных:
	 R1 = sigma N_авквариума = 13 (P1);
	 R2 = sigma N_авквариума = 13 (P4);
	 
	 Вариант 4:
1.1. Вывести все виды рыб из Бирмы или Полинезии
1.2. R  = pi вид_рыбы (sigma страна_происхождения = "Бирма" V страна_происхождения = "Полинезия" (P2));
1.3. SELECT вид_рыбы
	 FROM P2
	 WHERE страна_происхождения = "Бирма" OR страна_происхождения = "Полинезия";
2.1. CREATE TABLE P11 (
	 N_авквариума INTEGER PRIMARY KEY,
	 общее_количество_рыб INTEGER,
	 температура FLOAT,
	 тип_аэрации VARCHAR(30) NOT NULL,
	 тип_флоры INTEGER NOT NULL,
	 );
	 INSERT INTO P11
	 SELECT * 
	 FROM P1;
	 На РА невозможно создать новую таблицу и вставить в неё данные, выборка нужных данных:
	 R  = pi N_авквариума, общее_количество_рыб, температура, тип_аэрации, тип_флоры (P1);
2.2. INSERT INTO P2
	 VALUES ("Карп", "мальки", 1234, "Российская Империя");
	 На РА невозможно вставить новые данные
	 
	Вариант 5:
1.1. Вывести вид рыбы и тип флоры для рыб из Поленезии
1.2. R1 = sigma страна_происхождения = "Полинезия" (P2);
	 R  = pi вид_рыбы, тип_флоры (R1 |><| P3);
1.3. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE a.страна_происхождения = "Поленезия";
2.1. SELECT N_авквариума
	 FROM P1
	 WHERE общее_количество_рыб  > 10
	 AND тип_флоры = 34;
	 R  = pi N_авквариума (sigma общее_количество_рыб > 10 ^ тип_флоры = 34 (P1));
2.2. SELECT N_авквариума
	 FROM P1
	 WHERE температура < 25
	 AND общее_количество_рыб > (SELECT AVG(общее_количество_рыб)
								 FROM P1);
	 На РА невозможно получить среднее значение, выборка приблизительных данных:
	 R  = pi N_авквариума (sigma температура < 25 ^ общее_количество_рыб  > 50 (P1));

	Вариант 6:
1.1. Вывести номера аквариумов, в которых общее количество рыб > 50 или рыб одного вида больше 5
1.2. R1 = pi N_авквариума (sigma общее_количество_рыб > 50 (P1));
	 R2 = pi N_авквариума (sigma количество >  5 (P4));
	 R  = R1 U R2;
1.3. SELECT N_авквариума
	 FROM P1
	 WHERE общее_количество_рыб > 50
	 UNION
	 SELECT N_авквариума
	 FROM P4
	 WHERE количество > 5;
2.1. SELECT N_авквариума, температура, общее_количество_рыб
	 FROM P1
	 ORDER BY температура;
	 На РА нет возможности упорядочивать выборку, приблизительная выборка:
	 R  = pi N_авквариума, температура, общее_количество_рыб (P1);
2.2. SELECT N_авквариума
	 FROM P1
	 WHERE температура = (SELECT MAX(температура)
						  FROM P1);
	 На РА невозможно получить максимальное значение, выборка приблизительных данных:
	 R = pi N_авквариума (sigma температура = 100 (P1));
	 
	Вариант 7:
1.1. Выбрать вид рыбы и её минимальную температуру, если её тип аэрации и флоры совпадает с типами аквариума 5
1.2. R1 = pi тип_аэрации, тип_флоры (sigma N_авквариума = 5 (P1));
	 R2 = P2 |><| P3;
	 R  = pi вид_рыбы, минимальная_температура (R2 / R1);
1.3. SELECT a.вид_рыбы, b.минимальная_температура
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE EXISTS (SELECT c.тип_аэрации, c.тип_флоры
				   FROM P1 c
				   WHERE c.N_авквариума = 5
				   AND c.тип_аэрации = b.тип_аэрации
				   AND c.тип_флоры = b.тип_флоры);
2.1. SELECT код_содержания, тип_флоры, тип_аэрации
	 FROM P3
	 WHERE минимальная_температура = 17
	 AND максимальная_температура = 24;
	 R1 = sigma минимальная_температура = 17 ^ максимальная_температура = 24 (P3);
	 R  = pi код_содержания, тип_флоры, тип_аэрации (R1);
2.2. SELECT a.N_авквариума
	 FROM P4 a
	 WHERE NOT EXISTS (SELECT b.вид_рыбы
					   FROM P2 b
					   WHERE NOT EXISTS (SELECT c.N_авквариума
										 FROM P4 c
										 WHERE a.N_авквариума = c.N_авквариума
										 AND b.вид_рыбы = c.вид_рыбы));
	 R1 = pi вид_рыбы (P2);
	 R  = pi N_авквариума (P4 / R1);
	 
	Вариант 8:
1.1. Вывести список аквариумов с номерами < 10
1.2. R  = pi N_авквариума (sigma N_авквариума < 10 (P1));
1.3. SELECT N_авквариума
	 FROM P1
	 WHERE N_авквариума < 10;
2.1. SELECT N_авквариума
	 FROM P1
	 WHERE тип_аэрации = "автоматический"
	 AND температура IN (15, 25, 35, 10);
	 R1 = sigma тип_аэрации = "автоматический" ^ температура !принадлежит! (15, 25, 35, 10) (P1);
	 R  = pi N_авквариума (R1);
2.2. SELECT COUNT(*)
	 FROM P2
	 WHERE код_содержания = 345
	 AND страна_происхождения = "Полинезия";
	 На РА невозможно получить количество элементов, приблизительная выборка:
	 R1 = sigma код_содержания = 345 ^ страна_происхождения = "Поленезия" (P2);
	 R  = pi N_авквариума (R1);
	 
	Вариант 9:
1.1. Вывести номера аквариумов с температурой не меньше 20 градусов и общее количество рыб не больше 50, в которых живут гуппи
1.2. R1 = pi N_авквариума (sigma температура >= 20 ^ общее_количество_рыб <= 50 (P1));
	 R2 = pi N_авквариума (sigma вид_рыбы = "гуппи" (P4));
	 R  = R1 !пересечение! R2;
1.3. SELECT a.N_авквариума
	 FROM P1 a 
	 WHERE a.температура >= 20
	 AND общее_количество_рыб <= 50
	 AND N_авквариума IN (SELECT b.N_авквариума
					      FROM P4 b
					      WHERE вид_рыбы = "гуппи");
2.1. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE a.вид_рыбы LIKE "a%"
	 AND b.тип_аэрации = "автомат";
	 На РА невозможно проверить совпадение по подстроке, приблизительная выборка:
	 R1 = P2 |><| P3;
	 R2 = sigma вид_рыбы = "а" ^ тип_аэрации = "автомат" (R1);
	 R  = pi вид_рыбы (R2);
2.2. SELECT a.N_авквариума
	 FROM P1 a
	 WHERE общее_количество_рыб > 100 
	 OR 10 < ALL (SELECT b.количество
				  FROM P4 b
				  WHERE a.N_авквариума = b.N_авквариума);
	 R1 = pi N_авквариума (sigma общее_количество_рыб > 100 (P1));
	 R2 = pi N_авквариума (sigma общее_количество_рыб <= 100 (P1))
	 R3 = pi N_авквариума (sigma количество <= 10 (P4));
	 R  = R1 U (R2 - R3);
	
	Вариант 10:
1.1. Выбрать все аквариумы с автоматическим или полуавтоматическим типом аэрации
1.2. R  = pi N_авквариума (sigma тип_аэрации = "автоматический" V тип_аэрации = "полуавтоматический" (P1));
1.3. SELECT N_авквариума
	 FROM P1
	 WHERE тип_аэрации = "автоматический"
	 OR тип_аэрации = "полуавтоматический";
2.1. SELECT *
	 FROM P2
	 WHERE страна_происхождения IS NULL;
	 На РА нет аналога IS NULL, приблизительная выборка:
	 R  = sigma страна_происхождения = NULL (P2);
2.2. SELECT a.вид_рыбы
	 FROM P2 a
	 WHERE NOT EXISTS (SELECT b.N_авквариума
					   FROM P4 b
					   WHERE a.вид_рыбы = b.вид_рыбы);
	 R1 = pi вид_рыбы (P2);
	 R2 = pi вид_рыбы (P4);
	 R  = R1 - R2;
	 
	Вариант 11:
1.1. Вывести вид рыбы и тип флоры для рыб из Поленезии
1.2. R1 = sigma страна_происхождения = "Полинезия" (P2);
	 R  = pi вид_рыбы, тип_флоры (R1 |><| P3);
1.3. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE a.страна_происхождения = "Поленезия";
2.1. SELECT a.вид_рыбы, b.минимальная_температура, максимальная_температура, тип_флоры, тип_аэрации
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания;
	 R1 = P2 |><| P3;
	 R  = pi вид_рыбы, минимальная_температура, максимальная_температура, тип_флоры, тип_аэрации (R1);
2.2. SELECT c.N_авквариума, c.температура
	 FROM P2 a
	 JOIN P4 b
	 ON a.вид_рыбы = b.вид_рыбы
	 JOIN P1 c
	 ON b.N_авквариума = c.N_авквариума
	 WHERE a.страна_происхождения <> "Бирма";
	 R1 = sigma страна_происхождения != "Бирма" (P2);
	 R2 = R1 |><| P4 |><| P1;
	 R  = pi N_авквариума, температура (R2);
	 
	Вариант 12:
1.1. Вывести виды рыб, для которых тип флоры = 34, а минимальная температура 10
1.2. R1 = P2 |><| P3;
	 R2 = sigma тип_флоры = 34 ^ минимальная_температура = 10 (R1);
	 R  = pi вид_рыбы (R2);
1.3. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE b.тип_флоры = 34
	 AND b.минимальная_температура = 10;
2.1. SELECT a.вид_рыбы, b.вид_рыбы
	 FROM P2 a
	 JOIN P2 b
	 ON a.вид_рыбы < b.вид_рыбы
	 AND a.код_содержания = b.код_содержания;
	 R1 = P2 RENAME вид_рыбы AS вид_рыбы2;
	 R2 = P2 |><| R1;
	 R  = pi вид_рыбы, вид_рыбы2 (R2);
2.2. SELECT a.вид_рыбы
	 FROM P2 a
	 JOIN P3 b
	 ON a.код_содержания = b.код_содержания
	 WHERE минимальная_температура <= 24
	 AND максимальная_температура >= 28;
	 R1 = sigma минимальная_температура <= 24 ^ максимальная_температура >= 28 (P2 |><| P3);
	 R  = pi вид_рыбы (R1);